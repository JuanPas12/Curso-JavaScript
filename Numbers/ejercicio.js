let altura_cm = 185;
console.log(altura_cm);

let altura_m = 1.85;
console.log(altura_m);

let peso = 86.3;
console.log(peso);

let altura_m_r = Math.round(altura_m);
console.log(altura_m_r);

let peso_r = Math.round(peso);
console.log(peso_r);

let max_value_mas_1 = Number.MAX_VALUE + 1;
let max_value = Number.MAX_VALUE;
let iguales = max_value_mas_1 === max_value;

console.log(iguales);