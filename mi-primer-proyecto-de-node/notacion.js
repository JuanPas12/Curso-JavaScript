// Notación
// ; --> Delimitar el final de una linea
const b = 4;
b + 4;
//Al dia de hoy no pasa nada si no se usa el punto y coma, pero lo mejor para prevenir errores es utilizarlo

// . --> Se utiliza en los objetos para acceder a los atributos
//movil.ancura

// [] --> listas, arreglos o array
const ar = [1, 2, 3, 4];
console.log(ar[2]);

// () --> Funciones
function suma(a, b) {
    //se escribe la función
}
// {} --> Objetos, funciones y estructuras de control
const movil = {
    ancura: 5,
    altura: 10,
};

if (true) {
    //todo lo que haya entre llaves
}
